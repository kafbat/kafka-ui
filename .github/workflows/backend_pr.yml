name: "Backend: PR: Build and test"

on:

  pull_request:
    types: [ "opened", "reopened", "synchronize" ]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - uses: dorny/paths-filter@7267a8516b6f92bdb098633903c7f4657d1eb4ab # https://github.com/dorny/paths-filter/releases/tag/v3.0.3
        id: filter
        with:
          filters: |
            backend:
              - "build.gradle"
              - "gradle.properties"
              - "settings.gradle"
              - "gradle/libs.versions.toml"
              - "contract/**"
              - "contract-typespec/**"
              - "api/**"
              - "serde-api/**"

  build-and-test:
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    uses: ./.github/workflows/backend_tests.yml
    with:
      event_name: ${{ github.event_name }}
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
